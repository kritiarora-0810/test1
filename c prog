#include <stdio.h>
int main() {    

    int noo1, noo2, avg;
    
    printf("Enter two integers: ");
    scanf("%d %d", &noo, &noo2);

    // calculate the sum
    avg = noo1 + noo2;      
    
    printf("%d + %d = %d", noo1, noo2, avg);
    return 0;
}
