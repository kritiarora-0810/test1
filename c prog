#include <stdio.h>
int main() {    

    int nooo1, nooo2, avg;
    
    printf("Enter two integers: ");
    scanf("%d %d", &nooo, &nooo2);

    // calculate the sum
    avg = nooo1 + nooo2;      
    
    printf("%d + %d = %d", nooo1, nooo2, avg);
    return 0;
}
